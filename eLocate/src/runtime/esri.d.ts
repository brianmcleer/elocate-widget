// Editor-only open declarations for the __esri.X references this widget makes.
// Open interfaces merge with the real @arcgis/core types if they ever load.
export {}
declare global {
  namespace __esri {
    interface MapView { [key: string]: any }
  }
}
